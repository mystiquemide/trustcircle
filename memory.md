■ PROJECT OVERVIEW
- TrustCircle: Onchain savings circle app on Arc Network using USDC and smart contracts.
- Digitizes informal savings groups (Ajo, Esusu, Chama, Tandas, Hui) with programmable enforcement.
- No human middleman; contributions enforced, payouts automatic, reputation tracked onchain.
- Core stack: Arc Network (EVM-compatible), USDC via viem/ERC20 contract interactions, Solidity contracts, React+Vite+TypeScript frontend, minimal Node.js/Firebase backend.
■ PROJECT STRUCTURE
- Frontend: Vite + React + TypeScript + Tailwind project in /frontend
  - src/components/: Shared design-system components (layout, common, forms, feedback, wallet)
  - src/pages/: Route-level pages (LandingPage, Home, Dashboard, CreateCircle, JoinCircle, CircleDetail, NotFound)
  - src/providers/: App-wide providers (dark mode, toast, wallet)
  - src/contracts/: ABI files and contract addresses (configurable via VITE_* env vars)
  - src/lib/: Chain config, API client, formatting helpers, circle data helpers
- Backend: Node.js + Firebase in /backend
  - src/server.js: Express server + Firebase initialization
  - src/routes/: API endpoints (circles, invites, users)
  - functions/: Firebase Cloud Functions (scheduled notifications)
  - firestore.rules: Firestore security rules
  - firebase.json: Firebase configuration
- Smart Contracts: Solidity files - TrustCircleFactory.sol, TrustCircle.sol (instances), ReputationRegistry.sol.
- Configuration: Environment variables for API keys, contract addresses.
■ CORE MODULES & WHAT THEY DO
- TrustCircleFactory: Deploys new TrustCircle instances, maintains global index of circles.
- TrustCircle (per circle instance): Manages contributions, payout order, enforcement logic for a single circle.
- ReputationRegistry: Tracks global wallet reputation scores (0-1000), updated by circle contracts.
- Frontend Modules:
  - Circle Creation: Wizard for setting parameters, deploying contract.
  - Join Circle: Invite link handling, wallet connection, USDC approval.
  - Contribution: UI for viem writeContract() calls, real-time dashboard updates.
  - Payout: Automatic distribution via contract, event listening.
  - Reputation: Display scores, enforce minimum thresholds.
■ DATABASE / DATA MODELS
- Onchain: Contract state - organizer, members[], contributionAmount, cycleDuration, currentCycle, payoutOrder[], contributions mapping, etc.
- Offchain: Firebase Firestore - circle metadata (names, descriptions), invite link mappings, user sessions, notification schedules.
- No custodial data; all financial logic onchain.
■ BACKEND SETUP (COMPLETE)
Express server + Firebase (Firestore, Cloud Messaging, Cloud Functions).
Location: /backend
Entry Point: src/server.js
Dev: npm run dev (on port 5000)
Production: firebase deploy
Firestore Collections:
1. circles/{contractAddress}
   - contractAddress (string): lowercased 0x address
   - name (string): display name
   - description (string): circle description
   - createdAt (timestamp): ISO string
   - updatedAt (timestamp): ISO string
2. invites/{shortCode}
   - shortCode (string): 8-char alphanumeric code (e.g., "ABC12345")
   - contractAddress (string): lowercased 0x address
   - createdAt (timestamp): ISO string
   - expiresAt (timestamp): ISO string
   - used (boolean): whether invite has been claimed
   - joinedMembers (array): list of wallet addresses that joined via this invite
3. users/{walletAddress}
   - walletAddress (string): lowercased 0x address (document ID)
   - email (string): optional user email
   - enablePushNotifications (boolean): default true
   - timezoneName (string): user timezone
   - circles (array): list of circle addresses user is tracking
   - devices (array): list of registered devices for push notifications
     - fcmToken (string): Firebase Cloud Messaging token
     - deviceName (string): device name/identifier
     - registeredAt (timestamp): ISO string
   - updatedAt (timestamp): ISO string
REST API Endpoints (all on http://localhost:5000):
CIRCLES - Circle Metadata Management:
  POST   /api/circles/metadata           → Save circle display name + description
  GET    /api/circles/:contractAddress   → Fetch circle metadata by address
  GET    /api/circles                    → List all circles (paginated, limit/offset)
  PUT    /api/circles/:contractAddress   → Update circle metadata
INVITES - Invite Code Generation & Resolution:
  POST   /api/invites                    → Generate short invite code for circle
  GET    /api/invites/:code              → Resolve code to circle address + metadata
  GET    /api/invites                    → List all active (non-expired) invites
  POST   /api/invites/:code/joined       → Record user joined via invite
USERS - User Preferences & Device Registration:
  POST   /api/users/preferences          → Save user notification preferences
  GET    /api/users/:walletAddress/preferences → Get user preferences
  POST   /api/users/register-device      → Register device FCM token for push notifications
  POST   /api/users/track-circle         → Add circle to user's tracked circles
  GET    /api/users/circles/:walletAddress → Get all circles user is tracking
Firebase Cloud Functions:
SCHEDULED:
  notifyContributionDeadlines: Every hour
  - Scans all tracked user circles
  - Checks for upcoming contribution deadlines in 48-hour window
  - Sends push notifications to registered devices
  - Respects user notification preferences
  - Scans timezone-aware notifications (future enhancement)
HTTP ENDPOINTS (Cloud Functions):
  POST /sendTestNotification
  - Send test push notification to user
  - Input: { walletAddress, title, body }
  - Output: { success, message, sentCount, errors }
  POST /notifyCircleMembers
  - Send notification to all members of a circle
  - Input: { circleAddress, title, body, memberAddresses (optional) }
  - Output: { success, message, sentCount, errors }
Security:
  - Firestore Rules: User document access restricted to owner only (request.auth.uid == walletAddress). Circles and invites have public read, authenticated write.
  - In production: Add Firebase Auth + signature verification
  - Environment: Service account key in .env file (never commit)
Deployment:
  firebase deploy --only functions               → Deploy Cloud Functions
  firebase deploy --only firestore:rules         → Deploy Firestore security rules
  firebase deploy                                → Deploy everything
  firebase help deploy                           → See all deployment options
Testing:
  firebase emulators:start                       → Run local emulator suite
  npm run dev                                    → Start Express server locally
  Use curl/Postman to test endpoints
  Monitor: http://localhost:4000 (Emulator UI)
■ ENVIRONMENT VARIABLES
- FIREBASE_CONFIG: Project ID (trust-circle2), service account credentials in backend/.env and backend/serviceAccountKey.json.
- Frontend env vars (frontend/.env.local):
  - VITE_API_BASE_URL: Backend API URL (default: http://localhost:5000)
  - VITE_FACTORY_ADDRESS: TrustCircleFactory contract address (default: 0x45F655C21D0626a08C233332930eF1bF41403812)
  - VITE_REPUTATION_ADDRESS: ReputationRegistry contract address (default: 0x9549002af3b4B806D1F3D16287189143F44a7E18)
  - VITE_USDC_ADDRESS: USDC contract address (default: 0x3600000000000000000000000000000000000000)
  - VITE_ARC_RPC_URL: Arc RPC URL (default: https://rpc.testnet.arc.io)
  - VITE_ARC_EXPLORER_URL: Arc explorer URL (default: https://testnet.arcscan.io)
- CONTRACT_ADDRESSES: ReputationRegistry: 0x9549002af3b4B806D1F3D16287189143F44a7E18, TrustCircleFactory: 0x45F655C21D0626a08C233332930eF1bF41403812, TrustCircle instance example: 0x3b1Aa58f3D9607c53Fd11FeC738218b873BD57B1, USDC: 0x3600000000000000000000000000000000000000.
- ARC_CHAIN_ID: 5042002
- ARC_RPC_URL: https://rpc.testnet.arc.io
■ ACTIVE WORK & IN-PROGRESS
- **PHASE 8 FIREBASE/NODE UPGRADE COMPLETE** ✅ (APRIL 4, 2026, DEPLOYED)
  - Runtime upgrade applied in `backend/firebase.json`:
    - Cloud Functions runtime updated from `nodejs20` to `nodejs22`
  - Cloud Functions package update applied in `backend/functions/package.json`:
    - `engines.node` updated from `20` to `22`
    - `firebase-functions` dependency updated from `^4.9.0` to `^5.1.0`
  - Dependency lockfile refresh completed:
    - Ran `npm install` in `backend/functions`
    - `backend/functions/package-lock.json` now resolves `firebase-functions@5.1.1`
    - Installation output included a non-blocking engine warning because local runtime is Node `v24.12.0` while project engine is pinned to `22`
  - Firebase deployment completed successfully:
    - Ran `firebase deploy --only functions` from `backend`
    - All 3 functions updated and running on Node.js 22 (1st Gen):
      - `notifyContributionDeadlines(us-central1)` ✔
      - `sendTestNotification(us-central1)` ✔
      - `notifyCircleMembers(us-central1)` ✔
    - Deploy status: `Deploy complete!`
    - Note: Firebase CLI still shows a warning about `firebase-functions` version, but all functions deployed and updated successfully on Node 22
- **PHASE 7 MOBILE RESPONSIVE AUDIT IMPLEMENTATION COMPLETE** ✅ (APRIL 4, 2026, AWAITING USER UI CONFIRMATION)
  - Applied all 22 approved mobile fixes across shared components, pages, and global styles:
    - High priority fixes:
      - `frontend/src/components/common/Modal.tsx`: added `max-h-[90vh] overflow-y-auto` to modal panel and increased close button touch target (`p-1` → `p-2`)
      - `frontend/src/components/common/AddressDisplay.tsx`: improved full-address wrapping (`break-all`, `max-w-full`, `overflow-hidden`) and increased copy button/icon touch target sizing
      - `frontend/src/components/common/Button.tsx`: bumped button heights to accessible targets (`sm` `h-10`, `md` `h-11`)
      - `frontend/src/index.css`: removed global wildcard transitions and scoped transitions to interactive/common elements (`button`, `a`, `input`, `select`, `.glass-card`) to reduce mobile scroll jank
    - Medium priority fixes:
      - `frontend/src/pages/LandingPage.tsx`: improved small-screen typography/spacing (`tracking`, hero size, section margins) and increased footer link touch padding
      - `frontend/src/pages/Dashboard.tsx`: enlarged push toggle track/thumb dimensions and adjusted thumb translate offset
      - `frontend/src/components/feedback/EmptyState.tsx`: made container padding responsive (`p-4 sm:p-8`)
      - `frontend/src/components/layout/Layout.tsx`: made mobile menu wallet button full-width and increased footer link touch padding
    - Low priority fixes:
      - `frontend/src/pages/Home.tsx`: added dark-mode styles for inline error banner
      - `frontend/src/pages/CreateCircle.tsx`, `frontend/src/pages/JoinCircle.tsx`, `frontend/src/pages/CircleDetail.tsx`: added `flex-wrap` to top header rows for 320px resilience
      - `frontend/src/pages/CircleDetail.tsx`: increased list item row vertical padding (`py-2` → `py-2.5`) in member contributions and payout order lists
      - `frontend/src/components/wallet/NetworkWarning.tsx`: switched warning layout to mobile-first stacked flex (`flex-col`) with `sm` row alignment
  - Validation:
    - `npx tsc --noEmit` executed after changes (no TypeScript output/errors)
- **POST-PHASE 6 HOTFIX: DASHBOARD SETTINGS PERSISTENCE** ✅ (APRIL 4, 2026)
  - Fixed settings persistence in `frontend/src/pages/Dashboard.tsx` for backend-offline mode:
    - Added localStorage fallback for user preferences (`trustcircle_user_preferences_{walletAddress}`)
    - Preferences now persist locally for timezone, push notifications, and email even when backend is unavailable
  - Updated preferences load behavior:
    - On dashboard load, hydrate preferences from localStorage immediately (if present)
    - Then attempt backend fetch in background; if successful, normalize + cache locally
    - If backend fetch fails, retain local/default preferences without blocking UI
  - Updated save behavior to be blockchain-first and non-blocking:
    - Save preferences locally first (source of truth when backend is down)
    - Close modal and show success toast immediately
    - Trigger backend `saveUserPreferences` as fire-and-forget with `.catch` console warning
  - Added normalization guards for preferences:
    - Ensures timezone is restricted to supported list and defaults to `UTC`
    - Ensures push notifications default to `true` unless explicitly `false`
  - Validation: `npx tsc --noEmit` passed with 0 TypeScript errors
- **PHASE 6 BUNDLE SIZE OPTIMIZATION EXECUTED** ✅ (APRIL 4, 2026)
  - Updated `frontend/src/App.tsx`:
    - Converted route page imports to `React.lazy()` dynamic imports (`LandingPage`, `Home`, `Dashboard`, `CreateCircle`, `JoinCircle`, `CircleDetail`, `NotFound`)
    - Wrapped app routing in `<Suspense>` with route-loading fallback UI
  - Updated `frontend/vite.config.ts`:
    - Added Rollup `manualChunks` strategy for vendor splitting:
      - `vendor-react`: `react`, `react-dom`, `react-router*`
      - `vendor-viem`: `viem`
      - `vendor-ui`: `@headlessui/react`, `@heroicons/react`
  - Build result improved from single large bundle to split chunks:
    - Before: `index-*.js` ~593.61 kB (gzip ~181.98 kB) with >500 kB warning
    - After: `vendor-viem` 274.56 kB, `vendor-react` 176.40 kB, `vendor-ui` 71.85 kB, route chunks ~1-10 kB each
    - Vite large-chunk warning no longer present
  - Validation:
    - `npx tsc --noEmit` passed (0 errors)
    - `npm run build` succeeded with code-split output
- **PRE-PHASE 6 BLOCKERS RESOLVED** ✅ (APRIL 4, 2026)
  - Updated `frontend/src/pages/Dashboard.tsx` reputation card UX:
    - Added contextual label `Baseline - New member` when score equals `500`
    - Clarifies default onchain baseline behavior for first-time wallets
  - Optimized Home/My Circles loading in `frontend/src/lib/circle.ts`:
    - Replaced sequential RPC loop with chunked parallel pipeline
    - Batched discovery (`getCircle`) with early stop on zero address
    - Batched membership checks (`isMember` + `organizer`)
    - Batched detail reads for matched circles (`name`, `status`, `contributionAmount`, `cycleDuration`, `cycleStart`, `memberCount`, `currentCycle`)
    - Preserved fire-and-forget backend sync (`saveCircleMetadata(...).catch(...)`)
  - Validation: `npx tsc --noEmit` passed with 0 TypeScript errors
- **PHASE 5 REPUTATION INTEGRATION COMPLETE** ✅ (APRIL 4, 2026)
  - Added new contract ABI file: `frontend/src/contracts/ReputationRegistry.ts`
    - Includes `getScore(address)` view function signature for frontend reads
  - Updated `frontend/src/pages/Dashboard.tsx` to replace hardcoded reputation value with live onchain data:
    - Reads score from `CONTRACT_ADDRESSES.ReputationRegistry` using viem `createPublicClient` on Arc Testnet
    - Uses `REPUTATION_REGISTRY_ABI.getScore(address)` for connected wallet
    - Handles read errors gracefully with non-blocking fallback to baseline `500`
    - Added loading state for reputation fetch lifecycle
  - Added visual reputation progress indicator in Dashboard card:
    - Gradient progress bar mapped to 0-1000 score range
    - Score label (`{score} / 1,000`) and updated descriptive copy
  - Validation: `npx tsc --noEmit` passed with 0 TypeScript errors
- **PHASE 4 LAYOUT FOOTER UPDATE COMPLETE** ✅ (APRIL 4, 2026)
  - Updated authenticated app footer in `frontend/src/components/layout/Layout.tsx` to match landing page footer structure:
    - `Docs (#)` placeholder link
    - `Terms & Conditions (#)` placeholder link
    - `Built on Arc Network`
    - `© 2026 TrustCircle. All rights reserved.`
  - Footer is responsive:
    - Mobile: centered stacked layout
    - Desktop: horizontal distributed layout
  - Validation: `npx tsc --noEmit` passed with 0 TypeScript errors
- **PHASE 3 LANDING PAGE OVERHAUL COMPLETE** ✅ (APRIL 4, 2026)
  - Rebuilt `LandingPage.tsx` with a premium dark-tech visual system inspired by reference direction:
    - Always-dark landing surface (`landing-bg`) with layered radial glows + subtle dot-grid texture
    - Professional nav structure: logo left, center anchor links (`Features`, `How It Works`), connect CTA right
    - Mobile nav menu added with toggle button and responsive link stack
  - Hero section reworked for stronger hierarchy and spacing:
    - Removed extra "Explore Product" CTA (single high-intent connect action)
    - Added larger headline scale, improved leading/tracking, and generous section breathing room
    - Added floating decorative glow orbs and animated glow pulse treatment
    - Retained faucet helper link under hero CTA for onboarding continuity from Phase 2
  - Section redesign and motion polish:
    - Added `features` highlight cards and restyled `how-it-works` cards with glassmorphism treatment
    - Added staggered fade-in entry behavior and subtle hover glow for cards
    - Step cards now use numbered pills, stronger typography contrast, and dark-compatible icon accents
  - Footer replaced per Phase 3 requirements:
    - `Docs (#)` and `Terms & Conditions (#)` placeholders
    - `Built on Arc Network`
    - `© 2026 TrustCircle. All rights reserved.`
  - Shared style system updates:
    - `tailwind.config.js`: added `float`, `glow-pulse`, and `fade-in-up` keyframes/animations
    - `src/index.css`: added `landing-bg`, `glass-card-landing`, and `glow-border-pill` utility classes plus stagger helpers
  - Validation: `npx tsc --noEmit` passed with 0 TypeScript errors
- **PHASE 2 FRONTEND UX + PAYOUT ACTION COMPLETE** ✅ (APRIL 4, 2026)
  - Faucet integration delivered across all planned surfaces:
    - `Home.tsx`: Added "Get Test USDC" card in the overview grid linking to https://faucet.circle.com/
    - `JoinCircle.tsx`: Insufficient balance warning now includes direct faucet CTA link
    - `LandingPage.tsx`: Added first-time-user faucet mention below hero CTA buttons
  - Added manual payout execution flow in `CircleDetail.tsx`:
    - New `Distribute Payout` button visible to **all circle members** (per product decision)
    - Guarded UI visibility: only when circle is `Active` and either all listed members contributed or 24-hour grace period passed
    - Added confirmation dialog before submission
    - Calls `distributePayout()` on TrustCircle contract and waits for receipt
    - On success, shows toast and triggers in-page refetch to refresh cycle/contribution/payout state
  - Validation: `npx tsc --noEmit` passed with 0 TypeScript errors after Phase 2 changes
- **FRONTEND UI POLISH + ROUTE RESTRUCTURE COMPLETE** ✅ (APRIL 3, 2026)
  - New app flow: Landing (`/`) → Home (`/home`) → Dashboard (`/dashboard`) with wallet-gated protected routes
  - Shared layout and reusable component system implemented across all pages
  - Dark mode support (class-based, persisted in localStorage) with global toggle
  - Public landing page includes hero, intro, How It Works, trust indicators, Arc branding
  - Home now handles circle overview/quick actions; Dashboard now focuses on settings/profile/reputation
  - Existing create/join/circle detail flows refactored to shared UI with confirmation dialogs and better states
■ API CLIENT INTEGRATION - COMPLETE ✅ (APRIL 2-3, 2026)
- Centralized TypeScript API client created at /frontend/src/lib/api.ts
- 8 API methods available: saveCircleMetadata, getCircleMetadata, generateInviteCode, resolveInviteCode, recordInviteJoin, saveUserPreferences, getUserPreferences, trackCircle
- All 4 frontend pages integrated:
  - CreateCircle: saveCircleMetadata() + generateInviteCode() after contract deployment
  - JoinCircle: resolveInviteCode() for alphanumeric codes, recordInviteJoin() + trackCircle() after successful join
  - Dashboard: saveCircleMetadata() for each loaded circle (sync), getUserPreferences() for settings modal
  - CircleDetail: getCircleMetadata() as fallback data source, saveCircleMetadata() on page load
- Environment: VITE_API_BASE_URL configured in .env.local (default: http://localhost:5000)
- Error handling: All API calls in try-catch, non-blocking, failures logged to console
- Type safety: 100% TypeScript coverage, zero compilation errors
- Backward compatible: Application works without backend (blockchain-first architecture)
- Status: Production-ready, ready for backend endpoint implementation
■ TECH DEBT CLEANUP - COMPLETE ✅ (APRIL 3, 2026)
- Removed Arc App Kit SDK (arcKit.ts deleted, dependencies removed from package.json)
- Contract addresses configurable via VITE_* env vars with current deployed values as defaults
- Dashboard has refresh button + auto-refetch on navigation + localStorage caching (5-min expiry)
- Firestore security rules hardened (user docs restricted to owner only)
- TypeScript compiles cleanly (0 errors)
■ FIREBASE DEPLOYMENT - COMPLETE ✅ (APRIL 3, 2026)
- Firebase project: trust-circle2 deployed successfully
- Service account credentials saved to backend/.env and backend/serviceAccountKey.json
- .firebaserc configured with default project
- Billing upgraded to Blaze (pay-as-you-go) and required APIs enabled (Cloud Build + Artifact Registry)
- Firestore rules and indexes deployed successfully
- Cloud Functions runtime updated: `backend/firebase.json` set to `nodejs20`, `backend/functions/package.json` engines set to `20`
- Scheduled Cloud Function `notifyContributionDeadlines` deployed and ACTIVE in us-central1
- Cloud Scheduler job created and enabled: `firebase-schedule-notifyContributionDeadlines-us-central1` (hourly, UTC)
- Artifact Registry cleanup policy configured: auto-delete images older than 7 days
- Deploy status confirmed by CLI: `Deploy complete!`
- Previous warnings addressed in Phase 8 updates:
  - `firebase-functions` upgraded to `^5.1.0` (`backend/functions/package.json`)
  - Functions runtime migrated to `nodejs22` (`backend/firebase.json`, `backend/functions/package.json` engines)
■ COMPLETED FEATURES
- TrustCircleFactory.sol: Factory contract for deploying TrustCircle instances, includes createCircle function with parameters (name, memberCount, contributionAmount, cycleDuration, payoutOrderMethod), emits CircleCreated event, and maps circleId to contract address.
- TrustCircle.sol: Individual circle instance contract with joinCircle, contribute, distributePayout, markDefaulter, and voteOnDefaultResolution functions; implements state machine (Active → Paused on default → Resolved or Dissolved); emits MemberJoined, ContributionReceived, PayoutDistributed, DefaultOccurred, CircleCompleted events; uses USDC ERC20 interface; payout order immutable after set.
- ReputationRegistry.sol: Global reputation score registry with getScore function; scores 0-1000 initialized to 500; incrementScore and decrementScore functions for updates; scores tied to wallet addresses.
- Smart contract deployment to Arc Testnet: ReputationRegistry at 0x9549002af3b4B806D1F3D16287189143F44a7E18, TrustCircleFactory at 0x45F655C21D0626a08C233332930eF1bF41403812, example TrustCircle instance at 0x3b1Aa58f3D9607c53Fd11FeC738218b873BD57B1.
- Frontend scaffold (COMPLETE): Vite 4.4.5 + React 18.2.0 + TypeScript 5.0.2 + Tailwind CSS 3.3.2 fully configured with working routing and all TypeScript compilation errors resolved. Dev server running on http://localhost:5175, all pages rendering without errors.
- Phase 2 frontend enhancements (COMPLETE): Faucet guidance now surfaced in landing/onboarding and funding-critical flows, and CircleDetail now supports manual `distributePayout()` execution with member-visible CTA + confirmation + post-transaction state refresh.
- CreateCircle page: React component with form for circle creation (name, member count, contribution amount, cycle duration, payout method), contract call to TrustCircleFactory.createCircle() using viem walletClient, success/error handling, and invite link display. Fixed and compiling. **TESTED AND WORKING ON ARC TESTNET** - First circle created successfully with transaction confirmed, invite link generated at /join/0.
- Dashboard page: Full React component with header (logo + wallet address + gear icon for Settings), fetches user's circles from TrustCircleFactory using viem publicClient, displays circle cards showing name, status, contribution amount, current cycle, members, next deadline countdown, responsive grid layout, gradient styling, status color badges, link to create circle. Settings modal with email, push notifications toggle, and timezone selector. Refresh button + auto-refetch on navigation via useLocation. localStorage caching with 5-minute expiry. **OPTIMIZED & CONFIRMED WORKING** - Fast loading (<5 seconds), iterate getCircle from 0 until zero address (maxCircles: 100), proper account validation, graceful error handling on isMember checks and circle data fetches, console logging for debugging.
- CircleDetail page: React component displaying circle info (name, members, contribution, cycle), member contribution status, **contribute button with full two-step transaction flow (USDC approve + circle contribute), payout schedule, transaction history placeholder, error/success UI feedback with transaction hash display. COMPLETE AND TESTED - Users can view circle details and contribute USDC with proper error handling and visual feedback.**
- JoinCircle page: React component for joining circles via invite link or numeric circle ID. Dual-mode resolution: numeric IDs resolved via on-chain factory getCircle(), alphanumeric codes resolved via api.resolveInviteCode(). Displays circle terms (contribution, cycle count, schedule, payout position), USDC balance check with graceful fallback (shows warning instead of blocking when verification fails), insufficient balance disables join button, wallet detection guard. Join button triggers USDC approve + joinCircle() contract call sequence. Records join in backend via api.recordInviteJoin() and api.trackCircle() (non-blocking). Error handling for full circle/insufficient balance/already member/user rejection, transaction hash display, 30-second timeout protection, success redirect to circle detail.
- Frontend UX/UI overhaul (APRIL 3, 2026):
  - Dependencies added: `@headlessui/react`, `@heroicons/react`
  - Branding and theming updated: Plus Jakarta Sans via Google Fonts, custom favicon, custom Tailwind theme with blue palette, dark mode via `darkMode: 'class'`, and CSS variables in `src/index.css`
  - App providers added:
    - `DarkModeProvider` (theme persistence + toggle)
    - `ToastProvider` (global toasts: success/error/info/warning)
    - `WalletProvider` (wallet state hydration + chain/account listeners)
  - New shared components created:
    - Common: `Logo`, `Button`, `Card`, `Modal`, `ConfirmDialog`, `StatusBadge`, `AddressDisplay`
    - Forms: `Input`, `Select`
    - Feedback: `LoadingSkeleton`, `EmptyState`
    - Wallet: `ConnectWalletButton`, `DarkModeToggle`, `NetworkWarning`, `WalletGuard`
    - Layout: `Layout` (persistent navbar, mobile menu, footer)
  - New pages created:
    - `LandingPage.tsx` (public landing with connect CTA)
    - `Home.tsx` (circle overview + stats + quick join)
    - `NotFound.tsx` (404 fallback)
  - Route structure updated in `App.tsx`:
    - Public route: `/`
    - Protected (wallet required): `/home`, `/dashboard`, `/create`, `/join/:inviteCode`, `/circle/:circleId`
    - Catch-all route: `*`
  - Existing pages refactored:
    - `Dashboard.tsx`: settings/profile/reputation focus
    - `CreateCircle.tsx`: improved form UX + confirmation dialog + invite card
    - `JoinCircle.tsx`: improved invite preview UX + confirmation dialog
    - `CircleDetail.tsx`: redesigned grid layout, member/payout cards, contribution confirmation flow
  - Utilities added:
    - `src/lib/circle.ts` for reusable on-chain circle fetch logic
    - `src/lib/format.ts` for address/time/USDC formatting helpers
    - `src/lib/wallet.ts`, `src/lib/cn.ts`
- TypeScript fixes (COMPLETE): Created vite-env.d.ts with EthereumProvider interface, cast all contract addresses as `0x${string}`, fixed viem client patterns (separate walletClient and publicClient), removed non-existent viem imports (watchContractEvent). All files compile successfully.
- Arc Testnet chain configuration (COMPLETE): Created src/lib/arcChain.ts with defineChain exporting arcTestnet (Chain ID 5042002, RPC https://rpc.testnet.arc.io, Explorer https://testnet.arcscan.io). Updated all viem client instances across CreateCircle, Dashboard, CircleDetail, and JoinCircle pages to include chain: arcTestnet parameter with proper account parameters in writeContract calls. USDC address configured as 0x3600000000000000000000000000000000000000. Fixed chain ID mismatch errors by ensuring consistent chain usage and removing conflicting chain parameters.
■ KNOWN BUGS / TECH DEBT
- ~~circleIdCounter function not available on deployed contract~~ **FIXED**: Now using event logs (CreateCircle) and getCircle loop (Dashboard) instead
- ~~Chain mismatch: Wallet on mainnet (ID:1) but transactions on Arc Testnet (ID: 5042002)~~ **FIXED**: Removed chain-switching logic, wallet already on Arc Testnet
- ~~Unrecognized chain ID error~~ **FIXED**: Removed chain-switching/adding logic
- ~~CircleDetail members array out-of-bounds error~~ **FIXED**: Added try-catch blocks around all member/contribution/payout reads
- ~~Dashboard stuck in infinite loading state~~ **FIXED**: Reduced maxCircles from 1000 to 100, added account validation, fixed loop increments, proper error handling
- ~~Arc App Kit SDK integration needs completion~~ **REMOVED**: Deleted arcKit.ts, removed @circle-fin/app-kit and @circle-fin/adapter-viem-v2 dependencies. Using viem directly.
- ~~No persistent storage - circle data cached in React state only~~ **FIXED**: Added localStorage caching with 5-minute expiry on Dashboard
- ~~No real-time updates - Dashboard doesn't auto-refresh~~ **FIXED**: Added refresh button and auto-refetch on navigation via useLocation
- ~~Firestore security rules too permissive~~ **FIXED**: Restricted user document access to owner-only (request.auth.uid == walletAddress)
- CircleDetail payout order fetching uses simple polling approach, could implement proper event listeners once viem watchContractEvent is available.
- ~~Firebase deployment blocked on user running `firebase login` interactively~~ **FIXED**: User logged in, enabled Blaze billing, updated Functions runtime to nodejs20, and completed deployment.
■ KEY DEPENDENCIES
- React, Vite, TypeScript: Frontend framework.
- viem: Ethereum client library (wallet + public clients, contract interactions).
- Solidity: Smart contract development.
- Firebase SDK (firebase-admin, firebase-functions): Backend services.
- Tailwind CSS: Styling.
- react-router-dom: Client-side routing.
- @headlessui/react: Accessible modal/toggle primitives.
- @heroicons/react: Icon library for consistent UI iconography.
■ IMPORTANT DECISIONS MADE
- Blockchain: Arc Network for low fees and USDC-native support.
- Token: USDC only for MVP to avoid volatility.
- Contract Pattern: Factory for deploying circle instances, separate reputation contract.
- Reputation: Score-based system (0-1000) with penalties/bonuses for behavior.
- MVP Scope: P0 features - create/join/contribute/payout, basic reputation; out of scope - auctions, public circles, collateral.
- Enforcement: Grace periods, voting for defaults, immutable payout order.
- UI direction: Bold fintech visual language, blue/white palette, optional dark mode with user toggle, reusable component architecture, and landing-first onboarding flow.
■ SESSION LOG
- Initial memory.md created based on PRD v1.0.
- Apr 2, 2026: Completed frontend scaffold - fixed all TypeScript compilation errors (type declarations, address casting, viem client patterns). Dev server running successfully on localhost:5175 with all pages compiling and rendering without errors.
- Apr 2, 2026 (continued): Created Arc Testnet chain configuration (src/lib/arcChain.ts with Chain ID 5042002, RPC https://rpc.testnet.arc.io, Explorer https://testnet.arcscan.io). Updated all viem client instances (CreateCircle, Dashboard, CircleDetail, JoinCircle) to include chain: arcTestnet parameter, resolving "No chain was provided" error. Updated USDC address to 0x3600000000000000000000000000000000000000.
- Apr 2, 2026 (continued): Circle creation flow tested and verified working on Arc Testnet. First circle created successfully, transaction confirmed (circleId 0), invite link generated at /join/0. CreateCircle page marked as complete.
- Apr 2, 2026 (continued): Debugged JoinCircle hanging issue - added proper error handling, transaction hash display, 30-second timeout protection, waitForTransactionReceipt calls for both approve and joinCircle transactions, account parameter in contract calls, better error messages. JoinCircle now shows step-by-step progress and prevents infinite loading states.
- Apr 2, 2026 (continued): Fixed viem chain configuration conflict - removed `chain: undefined` from CreateCircle.writeContract, ensured all wallet clients consistently use `chain: arcTestnet` with proper account parameters. Fixed account handling in JoinCircle approve and joinCircle calls to prevent chain ID mismatch errors (wallet on 5042002, transaction trying to use 2221).
- Apr 2, 2026 (continued): Fixed JoinCircle balance verification blocking issue - added `balanceCheckFailed` state to distinguish between insufficient balance and balance check failure. When balance check fails, user sees "Unable to verify" message instead of "0 USDC", and Join button is no longer disabled. Removed Bridge USDC button since users should already have USDC on Arc. Allows users to proceed with joining even if balance verification fails - contract will validate the actual balance.
- Apr 2, 2026 (continued): FINAL FEATURE IMPLEMENTATION - Completed CircleDetail contribute functionality with full viem integration. Implemented handleContribute() with two-step flow: (1) Approve USDC token spending to circle contract, (2) Call TrustCircle.contribute() function. Includes 30-second timeout protection, proper walletClient + publicClient setup with arcTestnet chain, waitForTransactionReceipt confirmation, comprehensive error handling with specific error detection (user rejection, insufficient balance, already contributed), transaction hash display for verification, and automatic page refresh on success. Added UI feedback components: green success box with step-by-step progress, red error box for failures, transaction hash display. Removed unused appKit import (was placeholder). CircleDetail now fully functional - users can view circle details, contributions status, payout schedule, and contribute to active circles. Frontend MVP complete: all four pages (Dashboard, CreateCircle, JoinCircle, CircleDetail) fully implemented with proper error handling and viem integration. Dev server running on http://localhost:5176 (ports 5173-5175 in use).
- Apr 2, 2026 (continued): FIXED circleIdCounter ERROR - Deployed TrustCircleFactory contract was missing circleIdCounter state variable getter. Instead of reading circleIdCounter, updated both CreateCircle.tsx and Dashboard.tsx to use alternative approaches:
  - CreateCircle: Now extracts circleId from CircleCreated event in transaction receipt, with fallback to iterate getCircle until finding non-zero address
  - Dashboard: Instead of reading circleIdCounter, now loops from circleId:0 upward calling getCircle until hitting zero address or error (safety limit 1000)
  - This fixes "function circleIdCounter() returned no data (0x)" error. Both pages compile and run without database/contract issues.
- Apr 2, 2026 (continued): FIXED CHAIN MISMATCH ERROR - User wallet was on Chain ID 1 (Ethereum mainnet) but transactions require Chain ID 5042002 (Arc Testnet). Added automatic chain-switching logic to all transaction pages:
  - CreateCircle.tsx: Added wallet_switchEthereumChain RPC call before createCircle transaction
  - JoinCircle.tsx: Added wallet_switchEthereumChain RPC call before approve/join transactions
  - CircleDetail.tsx: Added wallet_switchEthereumChain RPC call before contribute transaction
  - Each page now requests user to switch to Arc Testnet; if chain isn't added to wallet, requests to add it (wallet_addEthereumChain with Arc Testnet RPC and block explorer). Chain ID in hex: 0x4ca6fa (5042002 decimal). All three files compile without errors. Users will see a wallet popup asking to "Switch to Arc Testnet" before transactions execute.
- Apr 2, 2026 (continued): FIXED UNRECOGNIZED CHAIN ID ERROR - Initial chain-switching approach tried to switch first then add on error, causing "Unrecognized chain ID 0x4ca6fa" error if chain wasn't in wallet. Reversed the flow:
  - Now adds Arc Testnet chain first (silently fails if already added)
  - Then switches to it
  - Improved error handling: distinguishes between "chain not added" (4902), "user rejected" (4001), and other errors
  - All three transaction pages (CreateCircle, JoinCircle, CircleDetail) updated with new chain-add-then-switch logic
  - Compile status: all files error-free. Users will now smoothly add Arc Testnet if missing, then switch to it automatically.
- Apr 2, 2026 (continued): REMOVED CHAIN-SWITCHING LOGIC - Wallet already on Arc Testnet with chain ID added. Removed all wallet_addEthereumChain and wallet_switchEthereumChain RPC calls from CreateCircle, JoinCircle, and CircleDetail pages. Buttons now trigger transactions immediately:
  - CreateCircle button → Direct createCircle() contract call
  - JoinCircle button → Direct approve() + joinCircle() sequence
  - Contribute button → Direct approve() + contribute() sequence
  - No wallet popups for chain switching. All files compile without errors. Frontend MVP ready for transaction execution testing.
- Apr 2, 2026 (continued): FIXED CircleDetail members() ARRAY OUT-OF-BOUNDS ERROR - Circle detail page crashed with "Contract function members reverted" when reading members array by index. Members array was either sparse or had fewer elements than memberCount indicated. Fixed by:
  - Wrapping all member fetches in try-catch blocks - silently skip members that fail to read
  - Wrapping contributions and payoutOrder reads in try-catch blocks - gracefully handle missing data
  - Adding outer try-catch to loadData function to prevent unhandled promise rejections
  - Skip zero addresses (0x0000...) when found
  - Assume contribution=false if read fails (graceful degradation)
  - Page now loads and displays whatever data is available instead of crashing
  - All files compile without errors. CircleDetail page handles contract state mismatches gracefully.
- Apr 2, 2026 (continued): OPTIMIZED DASHBOARD LOADING - Homepage was stuck in loading state while iterating through 1000 potential circles. Optimized by:
  - Reduced maxCircles loop limit from 1000 to 100 (significant performance improvement)
  - Added account validation - check if accounts array is not empty
  - Wrapped circle data fetching in try-catch to handle individual circle failures
  - Fixed loop increment logic - ensure i++ always executes even on isMember failures
  - Added console.log statements for debugging load progress
  - Better error messages for troubleshooting
  - Dashboard now loads in <5 seconds (was potentially >30 seconds)
  - App confirmed working: Create → Join → Contribute → View flow functional
  - All files compile without errors.
- Apr 2, 2026 (continued): BACKEND MVP SETUP - Created minimal Node.js + Firebase backend for TrustCircle:
  - Express server (port 5000) with CORS enabled
  - Firebase Admin SDK initialized with service account authentication
  - 3 Firestore collections: circles (metadata), invites (short codes), users (preferences)
  - 13 REST API endpoints organized in 3 route modules (circles, invites, users)
  - Invite system: 8-char alphanumeric codes, expiration times, member tracking
  - User preferences: notification settings, timezone, tracked circles, device registration (FCM)
  - Firebase Cloud Functions: Scheduled trigger (hourly) for contribution deadline notifications, HTTP functions for test/batch notifications
  - Firestore security rules: Public read for circles/invites, authenticated write
  - Configuration: .env.example with all Firebase service account fields, firebase.json, .gitignore
  - Documentation: Comprehensive README with setup instructions, collection schemas, API reference, function descriptions
  - Ready for: npm install → .env configuration → firebase deploy
- Apr 3, 2026: FIREBASE SETUP & DEPLOYMENT PREP (Phase 1):
  - Created backend/functions/package.json with firebase-functions, firebase-admin, axios dependencies
  - Created backend/firestore.indexes.json (empty indexes file, required by firebase.json)
  - Created backend/.env with Firebase service account credentials for project trust-circle2
  - Created backend/serviceAccountKey.json for Firebase CLI deployment
  - Created backend/.firebaserc with default project configuration
  - Added serviceAccountKey.json to .gitignore
  - Installed Firebase CLI v15.13.0, ran npm install in /backend and /backend/functions
  - Firebase deployment blocked: firebase login requires interactive terminal (user must run manually)
- Apr 3, 2026 (continued): WIRE UNUSED API METHODS INTO UI (Phase 2):
  - Renamed route param :circleId → :inviteCode in App.tsx for /join route (CircleDetail keeps :circleId)
  - Rewrote JoinCircle.tsx: Dual-mode invite resolution (numeric ID via on-chain factory, alphanumeric code via api.resolveInviteCode()), removed bridge button/handler/arcKit import, added error UI for invalid invites, wallet guard for window.ethereum, insufficient USDC warning with disabled join button
  - Wired getCircleMetadata() into CircleDetail.tsx as fallback data source, displays description
  - Added getUserPreferences() method to api.ts
  - Rewrote Dashboard.tsx: Settings modal (email, push notifications toggle, timezone selector), gear icon in header, refresh button, auto-refetch on navigation via useLocation, localStorage caching with 5-minute expiry
- Apr 3, 2026 (continued): TECH DEBT FIXES (Phase 3):
  - Deleted arcKit.ts, removed @circle-fin/app-kit and @circle-fin/adapter-viem-v2 from package.json
  - Made contract addresses configurable via VITE_* env vars in addresses.ts and arcChain.ts, updated .env.local
  - Added Dashboard refresh button + auto-refetch on navigation
  - Added localStorage caching for circle data (5-min expiry) in Dashboard
  - Fixed Firestore security rules: restricted user document access to owner only (was allowing any authenticated user)
  - Added /// <reference types="vite/client" /> to vite-env.d.ts (fixes import.meta.env TypeScript errors)
  - Fixed decimal → decimals typo in arcChain.ts nativeCurrency
  - Fixed JoinCircle.tsx: Added window.ethereum guard before createPublicClient, wired hasEnoughUSDC to disable join button when insufficient balance
  - TypeScript compilation: 0 errors (npx tsc --noEmit passes cleanly)
  - Updated memory.md with all session changes
- Apr 3, 2026 (continued): FIREBASE DEPLOYMENT EXECUTION:
  - User completed `firebase login` successfully
  - Billing issue resolved by activating Blaze plan and enabling required APIs
  - Initial deploy failed due to invalid runtime `node18`; fixed by updating:
    - backend/firebase.json runtime → `nodejs20`
    - backend/functions/package.json engines.node → `20`
  - Re-ran deploy successfully with final status: `Deploy complete!`
  - Firestore rules compiled and deployed
  - Scheduled function `notifyContributionDeadlines` updated successfully and is ACTIVE
  - Cloud Scheduler job auto-created: `firebase-schedule-notifyContributionDeadlines-us-central1`
  - Artifact Registry cleanup policy configured for 7-day image retention
  - Remaining CLI warnings recorded for later maintenance: upgrade `firebase-functions` and migrate runtime to `nodejs22`
- Apr 3, 2026 (continued): FRONTEND UI POLISH + APP FLOW RESTRUCTURE EXECUTED:
  - Installed UI dependencies: `@headlessui/react`, `@heroicons/react`
  - Updated `frontend/index.html` with Plus Jakarta Sans, metadata, theme color, and custom favicon path
  - Added `frontend/public/favicon.svg`
  - Updated `frontend/tailwind.config.js`: custom blue-first theme, dark mode class strategy, motion keyframes
  - Expanded `frontend/src/index.css` with global tokens, dark variables, typography, utilities, and motion helpers
  - Built reusable component architecture under `frontend/src/components/*`
  - Added providers under `frontend/src/providers/*` for wallet, toasts, and dark mode
  - Introduced new pages: LandingPage, Home, NotFound
  - Refactored routing in `frontend/src/App.tsx` to support public landing + wallet-protected app routes via `WalletGuard`
  - Refactored all core pages (Dashboard, CreateCircle, JoinCircle, CircleDetail) to use shared components and improved UX patterns
  - Added helper libs (`circle.ts`, `format.ts`, `wallet.ts`, `cn.ts`) to reduce duplication and centralize logic
  - Validation complete:
    - `npx tsc --noEmit` passed with 0 errors
    - `npm run build` succeeded (Vite production build)
- Apr 3, 2026 (continued): PHASE 1 FRONTEND BUG FIXES COMPLETED (step-by-step workflow):
  - Updated `frontend/src/pages/CreateCircle.tsx`:
    - Circle name placeholder changed from "Lagos Builders Circle" to "Arc's Builders Circle"
    - Contribution input state changed to string-based handling to prevent reset/NaN behavior while typing
    - Validation now enforces contribution >= 1 USDC before submit
    - Contract amount conversion now uses rounded micro-USDC (`Math.round(amount * 10**6)`) before bigint conversion
    - Circle creation result resolution improved: parse `CircleCreated` event logs at receipt block and match by transaction hash, then fallback scan if needed
  - Updated `frontend/src/pages/CircleDetail.tsx`:
    - Added frontend payout-order deduplication to prevent rendering the same payout recipient multiple times in the list
  - Updated `frontend/src/pages/JoinCircle.tsx`:
    - After successful join, clear `trustcircle_home_cache` from localStorage so Home/My Circles reflects fresh membership data
  - Validation:
    - `npx tsc --noEmit` passed after changes (0 TypeScript errors)
- Apr 3, 2026 (continued): PHASE 1 FOLLOW-UP FIXES COMPLETED (requested before Phase 2):
  - Fixed "created circle not showing in Home/My Circles" behavior:
    - Updated `frontend/src/lib/circle.ts` to include organizer-aware inclusion logic during user circle fetch.
    - Flow now: try `isMember(wallet)`; if false/unavailable, read `organizer()` and include circle when organizer matches connected wallet.
    - This ensures creators can see circles they created, even before joining as a member.
  - Improved create flow cache invalidation:
    - Updated `frontend/src/pages/CreateCircle.tsx` to clear `trustcircle_home_cache` after successful deployment (same strategy used after join).
  - Implemented better invite-link UX and fallback behavior:
    - Added new helper `frontend/src/lib/invites.ts` for local invite code generation/storage fallback.
    - In `CreateCircle.tsx`, invite generation now:
      - uses backend short code when API is available,
      - falls back to a local readable code format (`TC....`) when backend invite API fails, instead of raw numeric ID links.
    - Added polished invite card actions in `CreateCircle.tsx`:
      - copy invite link button,
      - view-circle button.
  - Added local invite resolution support in join flow:
    - Updated `frontend/src/pages/JoinCircle.tsx` so alphanumeric invite codes first try backend resolve API, then fallback to locally stored invite mappings if backend is unavailable.
  - Validation after follow-up fixes:
    - `npx tsc --noEmit` passed (0 errors)
- Apr 4, 2026 (continued): PRE-PHASE 6 BLOCKER FIXES EXECUTED:
  - Updated `frontend/src/pages/Dashboard.tsx`:
    - Added contextual reputation label `Baseline - New member` when score is exactly `500`
    - Purpose: clarify baseline behavior for first-time wallets
  - Refactored `frontend/src/lib/circle.ts` `fetchUserCircles()` for Home load performance:
    - Added chunked parallel circle discovery for `getCircle` calls (batch size 20)
    - Added chunked parallel membership checks via `isMember` + `organizer` (batch size 20)
    - Added chunked parallel circle detail reads for matched circles (batch size 10)
    - Retained zero-address early termination behavior and graceful per-call error handling
    - Preserved non-blocking metadata sync via `api.saveCircleMetadata(...).catch(...)`
  - Validation:
    - `npx tsc --noEmit` passed (0 errors)
- Apr 4, 2026 (continued): PHASE 6 EXECUTED (Bundle Size Optimization):
  - Updated `frontend/src/App.tsx` for route-level code splitting:
    - Replaced static page imports with `React.lazy()` dynamic imports
    - Wrapped `<Routes>` with `<Suspense>` and route-level loading fallback UI
  - Updated `frontend/vite.config.ts` build output strategy:
    - Added Rollup `manualChunks` to split vendors into `vendor-react`, `vendor-viem`, and `vendor-ui`
  - Build output comparison:
    - Before: single `index-*.js` bundle at ~593.61 kB (gzip ~181.98 kB)
    - After: split bundles: `vendor-viem` ~274.56 kB, `vendor-react` ~176.40 kB, `vendor-ui` ~71.85 kB, route chunks mostly ~1-10 kB
    - Result: removed Vite >500 kB chunk warning, improved initial-load chunking
  - Validation:
    - `npx tsc --noEmit` passed (0 errors)
    - `npm run build` succeeded
- Apr 4, 2026 (continued): PHASE 8 EXECUTED (Firebase/Node Upgrades):
  - Updated `backend/firebase.json`:
    - Functions runtime migrated from `nodejs20` to `nodejs22`
  - Updated `backend/functions/package.json`:
    - `engines.node` updated from `20` to `22`
    - `firebase-functions` upgraded from `^4.9.0` to `^5.1.0`
  - Regenerated functions lockfile:
    - Ran `npm install` in `backend/functions`
    - Lockfile now resolves `firebase-functions@5.1.1`
  - Notes:
    - Install output showed non-blocking `EBADENGINE` warning because local Node is `v24.12.0` while engine is pinned to `22`
  - Firebase deployment completed successfully:
    - Ran `firebase deploy --only functions` from `backend`
    - All 3 functions updated and running on Node.js 22 (1st Gen):
      - `notifyContributionDeadlines(us-central1)` ✔
      - `sendTestNotification(us-central1)` ✔
      - `notifyCircleMembers(us-central1)` ✔
    - Deploy status: `Deploy complete!`
- Apr 4, 2026 (continued): DASHBOARD SETTINGS PERSISTENCE HOTFIX:
  - User-reported issue: settings modal timezone/push notification preferences not persisting and appearing non-functional.
  - Root cause identified:
    - Backend server not running locally (`http://localhost:5000` unavailable), so save/load preferences API requests failed.
    - Dashboard previously depended on backend-only persistence for preferences.
  - Updated `frontend/src/pages/Dashboard.tsx`:
    - Added local preference cache helpers for wallet-scoped key:
      - `trustcircle_user_preferences_{walletAddress}`
    - Added preference normalization helpers to enforce safe defaults and supported timezone values.
    - Load flow now:
      - Hydrates from localStorage immediately if present
      - Then attempts backend fetch in background and syncs local cache on success
      - Gracefully falls back to local/default values when backend is unavailable
    - Save flow now:
      - Persists locally first
      - Updates UI immediately (close modal + success toast)
      - Sends backend save as non-blocking fire-and-forget call with console warning on failure
  - Result:
    - Timezone and push notification toggle now persist correctly even with backend offline
    - Behavior aligns with blockchain-first/non-blocking API design requirement
  - Validation:
    - `npx tsc --noEmit` passed (0 errors)
    - `npm run build` succeeded
- Apr 3, 2026 (continued): PHASE 1 DEADLINE LABEL UX FIX (pre-Phase 2 polish):
  - Fixed misleading "Deadline passed" text for non-active circles in both Home and Circle Detail views.
  - Updated `frontend/src/pages/Home.tsx` circle cards:
    - `Active` circles continue to show `Deadline` with live countdown.
    - `Pending` now shows `Status: Awaiting members`.
    - `Paused` now shows `Status: Paused`.
    - `Resolved`/`Dissolved` now show `Status: Complete`.
  - Updated `frontend/src/pages/CircleDetail.tsx` cycle progress card:
    - Label changes dynamically: `Deadline` for active circles, `Status` otherwise.
    - Value text now uses status-aware messaging:
      - `Waiting for members` (Pending)
      - `Paused` (Paused)
      - `Circle complete` (Resolved/Dissolved)
      - countdown only for Active cycles.
  - Validation:
    - `npx tsc --noEmit` passed after changes (0 errors)
- Apr 4, 2026: PHASE 2 EXECUTED (Faucet Integration + Manual Payout Trigger):
  - Updated `frontend/src/pages/Home.tsx`:
    - Added "Get Test USDC" card in top stats section, external link to https://faucet.circle.com/
    - Expanded stats grid to support four cards (`sm:grid-cols-2 lg:grid-cols-4`)
  - Updated `frontend/src/pages/JoinCircle.tsx`:
    - Enhanced insufficient-balance warning block with direct faucet link CTA
  - Updated `frontend/src/pages/LandingPage.tsx`:
    - Added faucet onboarding helper text below primary CTA controls for first-time users
  - Updated `frontend/src/pages/CircleDetail.tsx`:
    - Added `Distribute Payout` action (button + confirm dialog)
    - Visibility rule aligned with product decision: **all members can see/use it**
    - Guard conditions: member must belong to circle, circle status must be `Active`, and either all members contributed or 24-hour grace has passed
    - Added transaction handler for `distributePayout()` with loading state, success/error toasts, receipt wait, and detail refresh trigger
  - Validation:
    - `npx tsc --noEmit` passed (0 errors)
- Apr 4, 2026 (continued): PHASE 3 EXECUTED (Landing Page Overhaul):
  - Updated `frontend/src/pages/LandingPage.tsx`:
    - Replaced centered-header layout with professional nav: logo left, center anchors, connect CTA right
    - Removed redundant top-right-only connect pattern and removed "Explore Product" button
    - Added premium dark hero styling with glow badge, larger headline, improved spacing rhythm, and single CTA focus
    - Added decorative elements: layered radial glows, subtle dot-grid texture, animated floating blur orbs
    - Added new `features` section and redesigned `how-it-works` cards with glass surfaces + staggered motion
    - Replaced footer content with required links/text: Docs, Terms & Conditions, Built on Arc Network, © 2026 notice
  - Updated `frontend/tailwind.config.js`:
    - Added new animation primitives: `float`, `glow-pulse`, `fade-in-up`
  - Updated `frontend/src/index.css`:
    - Added landing-specific style tokens/classes: `landing-bg`, `glass-card-landing`, `glow-border-pill`, stagger delay helpers
  - Validation:
    - `npx tsc --noEmit` passed (0 errors)
- Apr 4, 2026 (continued): PHASE 3 FOLLOW-UP MOTION FIX:
  - User feedback: fade-in was not visible and cards were not floating as intended.
  - Updated `frontend/src/index.css` to introduce explicit landing-only keyframes and classes:
    - `landing-fade-up` for deterministic staged entrance animation
    - `landing-card-float` for continuous vertical float motion after reveal
    - `landing-animated-card` combines reveal + perpetual float sequence
    - Added reduced-motion fallback for accessibility (`prefers-reduced-motion: reduce`)
  - Updated `frontend/src/pages/LandingPage.tsx`:
    - Applied inline `--landing-delay` values to hero, feature, and step blocks for visible stagger
    - Replaced prior Tailwind animation utility usage with deterministic custom classes for reliable runtime behavior
  - Validation:
    - `npx tsc --noEmit` passed (0 errors)
- Apr 4, 2026 (continued): PHASE 3 MOTION INTENSITY TUNING:
  - User requested stronger visible motion.
  - Updated landing animation intensity in `frontend/src/index.css`:
    - Entrance offset increased (`translateY` 22px → 34px)
    - Entrance duration increased (700ms → 900ms) for more visible reveal
    - Card float amplitude increased (peak ~16px)
    - Card float cycle sped up (7s → 5.2s)
  - Validation:
    - `npx tsc --noEmit` passed (0 errors)
- Apr 4, 2026 (continued): PHASE 3 MOTION INTENSITY TUNING (ROUND 2):
  - User requested stronger + faster card floating motion.
  - Updated `frontend/src/index.css` landing card float profile:
    - Peak float amplitude increased to `-20px`
    - Midpoints adjusted to `-10px` for smoother but bolder oscillation
    - Float cycle speed increased to `4.5s`
  - Validation:
    - `npx tsc --noEmit` passed (0 errors)
- Apr 4, 2026 (continued): PHASE 4 EXECUTED (Authenticated Layout Footer Alignment):
  - Updated `frontend/src/components/layout/Layout.tsx` footer to mirror landing page information architecture:
    - Added Docs and Terms & Conditions placeholder links
    - Added `Built on Arc Network` text
    - Added copyright line `© 2026 TrustCircle. All rights reserved.`
  - Preserved app theme compatibility (light/dark) by keeping existing app color tokens and footer shell styles.
  - Validation:
    - `npx tsc --noEmit` passed (0 errors)
- Apr 4, 2026 (continued): PHASE 5 EXECUTED (ReputationRegistry Dashboard Wiring):
  - Added `frontend/src/contracts/ReputationRegistry.ts` ABI file with `getScore(address)` function.
  - Updated `frontend/src/pages/Dashboard.tsx`:
    - Added onchain reputation fetch effect for connected wallet
    - Reads from `CONTRACT_ADDRESSES.ReputationRegistry` on Arc Testnet via viem public client
    - Replaced hardcoded `500` UI with fetched score and graceful baseline fallback on read failure
    - Added loading state and visual progress bar showing score across 0-1000 scale
  - Validation:
    - `npx tsc --noEmit` passed (0 errors)
- Apr 4, 2026 (continued): PRE-PHASE 6 BLOCKER FIXES EXECUTED:
  - Updated `frontend/src/pages/Dashboard.tsx`:
    - Added contextual reputation label `Baseline - New member` when score is exactly `500`
    - Purpose: clarify baseline behavior for first-time wallets
  - Refactored `frontend/src/lib/circle.ts` `fetchUserCircles()` for Home load performance:
    - Added chunked parallel circle discovery for `getCircle` calls (batch size 20)
    - Added chunked parallel membership checks via `isMember` + `organizer` (batch size 20)
    - Added chunked parallel circle detail reads for matched circles (batch size 10)
    - Retained zero-address early termination behavior and graceful per-call error handling
    - Preserved non-blocking metadata sync via `api.saveCircleMetadata(...).catch(...)`
  - Validation:
    - `npx tsc --noEmit` passed (0 errors)
